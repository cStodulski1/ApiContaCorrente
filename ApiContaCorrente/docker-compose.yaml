version: "3.9"

services:

  app:
    build: .
    container_name: conta_corrente_app
    depends_on:
      - db
    environment:
      - ConnectionStrings__DefaultConnection=Data Source=app/storage/contacorrentedb.db
    ports:
      - "8080:8081"
    volumes:
      - .:/app

volumes:
  pgdata:
